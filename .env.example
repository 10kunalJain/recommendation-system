# Environment configuration for H&M Recommendation System

# Performance
OPENBLAS_NUM_THREADS=1

# API serving
API_HOST=0.0.0.0
API_PORT=8000

# MLflow experiment tracking (optional)
MLFLOW_TRACKING_URI=sqlite:///mlruns/mlflow.db
MLFLOW_EXPERIMENT_NAME=hm-recsys

# Data paths (override defaults in config.yaml)
# DATA_RAW_DIR=data/raw
# ARTIFACTS_DIR=artifacts
